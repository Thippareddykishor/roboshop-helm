component_name: cart

service:
  port: "8080"
  containerPort: "8080"

configmap_data:
  CATALOGUE_HOST: "catalogue"
  CATALOGUE_PORT: "8080"
  REDIS_HOST: "redis-dev.kommanuthala.store"

schema: 
  enabled: false

resources:
  requests:
    memory: "50Mi"
    cpu: "150m"
  limits:
    memory: "100Mi"
    cpu: "250m"